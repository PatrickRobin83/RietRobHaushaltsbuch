<UserControl x:Class="RietRobHaushaltsbuch.Modules.CarRefuelTracker.Views.AddBrandView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RietRobHaushaltsbuch.Modules.CarRefuelTracker.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Vertical">
            <TextBlock Text="Fahrzeugmarke hinzufügen" FontSize="30" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            <DockPanel Margin="20" VerticalAlignment="Top">
                <TextBlock Margin="0,0,15,0" FontSize="20" VerticalAlignment="Center" Text="Fahrzeugmarke: "/>
                <TextBox Height="25" FontSize="20" Text="{Binding BrandName}"/>
            </DockPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Command="{Binding CancelAddBrandCommand}" Content="Abbrechen" Margin="0,0,15,0"/>
                <Button Command="{Binding AddBrand}" Content="Speichern"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
